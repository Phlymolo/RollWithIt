{
    "name": "roll-with-it",
    "description": "a .NET and Angular app",
    "scripts": {
        "heroku-postbuild": "cd Client && npm install && npm run build --prod"
    },
    "engines": {
        "node": "16.x",
        "npm": "6.x"
    }
}